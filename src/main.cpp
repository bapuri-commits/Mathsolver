#include <iostream>
#include "Matrix.hpp"

int main() {
    std::cout << "MathSolver v0.1.0" << std::endl;
    /* test code
    Matrix<int> A(2, 2, 2);
    Matrix<int> B(2, 2, 5);
    Matrix<int> C(2, 2);
    C = A + B;
    C = A - B;
    Matrix<int> D(2, 2);

    */
    return 0;
}
